[project]
name = "btasio"
version = "0.1.0"
description = "btasio web UI (Vercel dry-run / demo)"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.32.0",
  "pydantic>=2.9.0",
]

[tool.vercel]
entrypoint = "app:app"
